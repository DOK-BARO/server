spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: "jdbc:mysql://mysql/bewineddev?serverTimezone=Asia/Seoul"
        username: ${MYSQL_USER}
        password: ${MYSQL_PASSWORD}